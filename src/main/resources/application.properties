spring.application.name=escola-areninha
# config do banco local
spring.datasource.url=jdbc:postgresql://localhost:5432/escola_areninha_db
spring.datasource.username=postgres
spring.datasource.password=34732044

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.sql.init.mode=always

# config do token jwt
# na hora de rodar em producao, passo isso por variavel de ambiente
api.security.token.secret=${JWT_SECRET:12345678}